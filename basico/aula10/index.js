// Listas de operadores Aritméticos por ordem de  presedência
/* Parênteses:

( ) - Os parênteses têm a mais alta precedência e são usados para alterar a ordem padrão de avaliação.
Exponenciação:

** - Representa a operação de exponenciação.
Multiplicação, Divisão, Resto da Divisão:

* - Multiplicação
/ - Divisão
% - Resto da divisão (módulo)
Adição e Subtração:

+ - Adição
- - Subtração
Operadores de Atribuição:

=, +=, -=, *=, /=, %= - Atribuição e operadores de atribuição combinados. */

// ++ operador de incremento VC NÃO PODE USAR UM OPERADOR DE INCREMENTO  EM CONST E ELE PODE SER USADO
// PÓS OU PRÉ    

let contador =1
console.log(contador++); // o valor  se colocar ++ antes ele vai incrementar e o valor vai ser dois
console.log(contador)