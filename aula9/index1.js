const nome = 'Daniel'; //string
const num1 = 10 // numeral
const num2 =10.52 //numeral
let nomeAluno; // undefined -> não aponta pra local nenhum da memória
let sobreNome = null // Nulo -> não aponta pra local nehuma da memória
// A diferença de null pra undefined é que o null  não aponta para nenhum lugar na memoria
const boolean = true // ele pode ter dois valores true ou false (USAMOS PARA VALORES LOGICOS, QUEREMOS FAZER ALGUM DESVIO CONDICIONAL)

console.log(typeof boolean, boolean) // colocando assim temos o tipo e o valor 
// esses alguns dos tipos primitivos