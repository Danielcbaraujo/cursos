// criamos uma função e colocamos ela dentro de uma variavel estamos criando uma "Função anonima"

const raiz = function (n) { //funçã0o anonima
   return n**0.5;
};

console.log(raiz(9))


const rquadrada = (n) => {   // funçãode seta
    return n** 0.5
}

console.log(rquadrada(64))